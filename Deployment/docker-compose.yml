# version: "3.9"

services:
  # proxy:
    # image: jc21/nginx-proxy-manager:latest
    # container_name: nginx_proxy
    # restart: always
    # ports:
      # - "80:80"
      # - "81:81"
      # - "443:443"
    # volumes:
      # - /root/docker/volumes/examone/data:/data
      # - /root/docker/volumes/examone/letsencrypt:/etc/letsencrypt

  examone:
    image: examone
    container_name: examone
    build:
      context: ..
      dockerfile: ./Deployment/Dockerfile.ExamOne
    ports:
      - "8080"
    volumes:
      - /root/docker/volumes/examone/content_root_path/wwwroot:/app/wwwroot
      - /root/docker/volumes/examone/resource:/app/Resource
      - /root/docker/volumes/examone/logs:/app/Logs
    restart: always
    environment:
      ASPNETCORE_ENVIRONMENT: "Production"
      CONNECTION_STRING: "Server=mssqlserver;Database=ExamOneDb;User=sa;Password=KNoSyl4rp61kACZ1;TrustServerCertificate=True"

  # mssqlserver:
    # image: mcr.microsoft.com/mssql/server
    # container_name: mssqlserver
    # user: root
    # ports:
      # - "1433:1433"
    # restart: always
    # environment:
      # SA_PASSWORD: "KNoSyl4rp61kACZ1"
      # ACCEPT_EULA: "Y"
      # MSSQL_PID: "Express"
    # volumes:
      # - /root/data/MSSQLSERVER/DATA:/var/opt/mssql/data

# meilisearch:
#   image: getmeili/meilisearch:v1.7
#   container_name: meilisearch
#   ports:
#     - "7700:8080"
#   volumes:
#     - /root/docker/volumes/meili_data:/meili_data
#   environment:
#     MEILI_MASTER_KEY: "d0552b41536279a0ad88bd595327b96f01176a60c2243e906c52ac02375f9bc1"
#   restart: always
#
  # redis:
    # image: redis
    # container_name: redis_server
    # ports:
      # - "6379:6379"
    # restart: always
    # command: redis-server --requirepass KNoSyl4rp61kACZ1
  
networks:
  default:
    name: examone_network
